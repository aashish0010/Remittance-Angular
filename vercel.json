{
  "installCommand": "npm install && chmod -R +x node_modules/.bin",
  "buildCommand": "npx ng build",
  "outputDirectory": "dist/remittance-angular/browser",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://remittance-api.fly.dev/api/$1"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
