{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      // Suppress EF Core SQL command logs entirely — query parameters can contain
      // hashed PINs and other sensitive values even when EnableSensitiveDataLogging
      // is off. Remove this override only in a local scratch environment.
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // NEVER put real credentials here. Supply the real connection string via the
    // CONNECTION_STRING environment variable (see Program.cs).
    "DefaultConnection": "Host=ep-cool-recipe-absh0t34-pooler.eu-west-2.aws.neon.tech; Database=neondb; Username=neondb_owner; Password=npg_Lp1xGtkB8cDv; SSL Mode=VerifyFull; Channel Binding=Require;"
  },
  "JwtSettings": {
    // NEVER put the real secret here. Supply it via the JWT_SECRET_KEY environment variable.
    "SecretKey": "CHANGE_ME_VIA_JWT_SECRET_KEY_ENV_VAR_MIN_32_CHARS",
    "Issuer": "RemittanceAPI",
    "Audience": "RemittanceApp",
    "ExpirationSeconds": 3600,
    "RefreshTokenExpirationDays": 1
  }
}
